# What is still not in git

Runnable layer is in this folder now. Local checks on the source zip:

- `vop_verification-of-payee`: 16 passed, 5 skipped
- `fa_copilot_derive`: 15 passed

Not committed, because the GitHub file API here is text-only:

- `fa_handover_human-to-human/VoP_tracker_2026-09-11.xlsx`
- `LN_Team_Handoff_BG.pdf` (chat attachment, not inside the zip)
- `vop_verification-of-payee/UBB_VoP_Handover_2026-09-09.html` (57 KB deck; add from the zip if you want it browsable from github.com)

The HTML is the file people open first. If this folder is the source of truth, put the deck here next.
